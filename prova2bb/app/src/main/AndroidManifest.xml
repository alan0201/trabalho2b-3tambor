<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prova2bb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.rankingPassadas" android:label="Ranking das Passadas"></activity>
        <activity
            android:name=".activity.adicionarCompetidor"
            android:label="@string/name_competidor" />
        <activity android:name=".activity.adicionarPassada" />
        <activity android:name=".activity.menuprincipal"
            android:label="3 Tambor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:label="Competidores"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>